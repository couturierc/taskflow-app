/**
 * Thin re-exports so consumers don't need to know about internal theme plumbing.
 */
export {
  Colors,
  Fonts,
  SchemeColors,
  type ColorScheme,
  type ThemeColorPalette,
} from "@/lib/theme";
